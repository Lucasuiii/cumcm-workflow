\begin{abstract}
% CUMCM-TODO: write the abstract last, after body results, validation, and limitations are stable.
\placeholder{先用一两句说明实际问题、主要数据或机制困难，以及全文采用的核心方法。}\par

% Wrap the values that ARE the answer in \keyresult{}; bold the answer, not every number.
\placeholder{围绕主要贡献给出最重要的结果锚点：定量题优先写关键数值、单位、比较或区间，并立即解释其含义；不要使用“针对问题一建模、针对问题二求解”式空泛流水账。}\par

\placeholder{最后概括已有的拟合、残差、可行性、敏感性、稳定性、基线或样本外证据中真正执行过且最能支撑结论的部分，并点明主要适用边界。}\par

% Keywords must name the actual object, model, data, or method. Never use workflow vocabulary as filler.
\noindent\textbf{关键词：}\paperkeywords
\end{abstract}
